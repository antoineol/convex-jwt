import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  // Temporary setting to help debugging what's wrong with convex JWT auth
  reactStrictMode: false,
};

export default nextConfig;
